# Use the same documentation Python as tox and CI.
version: 2
build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
sphinx:
  configuration: docs/conf.py
  fail_on_warning: true
formats:
  - pdf
  - epub
python:
  install:
    - requirements: docs/requirements.txt
